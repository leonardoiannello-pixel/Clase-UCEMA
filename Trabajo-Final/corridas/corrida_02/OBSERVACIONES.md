# Corrida 2: consolidación ejecutada y rechazada

Se invocó realmente `agente/cli.py consolidate` sobre la propuesta/manifest confiables de Corrida 1 y la carpeta de devoluciones indicada. La herramienta terminó con exit code 1 y estado `REJECTED`. Las horas exactas, rutas y argumentos están en `cli_invocacion.json`. No se ejecutó generate ni se reintentó la consolidación.

El usuario declaró una intervención humana real, realizada manualmente en Excel fuera del agente: A001, `team_L-A.xlsx`, `Discretionary Adjustment %`, +1,00 pp. Se observó el valor numérico **0,01** en `Detail!R2`; los restantes ajustes son cero. El agente no decidió ni editó ningún ajuste. Las cuatro copias reviewed preservan exactamente los bytes utilizados; tres son idénticas a Corrida 1. `reviewer_id` es routing sintético y no se atribuye a una persona real.

V4 rechazó 13 diferencias textuales en fórmulas protegidas de team_L-A: `Summary!B4`, `Summary!B6`, `Summary!B8`, `Detail!S2:S6` y `Detail!V2:V6`. En todas, la única diferencia observada es la eliminación de comillas en referencias a hojas: `'Detail'!` pasa a `Detail!` y `'Summary'!` a `Summary!`. Es compatible con serialización al guardar en Excel; no acredita una manipulación deliberada del humano. `diferencias_protegidas.json` conserva ambos textos. El comparador existente exige igualdad textual y rechazó la devolución. No se normalizaron fórmulas, no se sustituyó el archivo y no se cambió V4 para eludir el control.

**No existe consolidated_final.xlsx de esta corrida.** `validation_exceptions.json` sí se generó y se archivó. El reporte del CLI contiene `budgets: []` y `outputs_generados: []`: en la ruta de rechazo, esa lista no enumera el log diagnóstico existente. El registro conserva por separado los archivos de evidencia producidos. No se deben interpretar los campos vacíos como ausencia de excepciones ni como aprobación.

Los salarios y porcentajes Proposed en los reviewed permanecen iguales al manifest. El Proposed Compa Ratio sigue preservado en el manifest original intacto; no hay consolidado para verificar su columna ni Final. Los controles posteriores de Final y recálculo de budgets por la consolidación **no pudieron completarse** porque la validación previa bloqueó el proceso.

Como observación separada, los valores cacheados por Excel en el reviewed de A001 muestran Final Salary **ARS 1.937.600**, Final Compa Ratio **0,8970370370370371** y Final Increase **0,384**. El Summary de Team Alpha muestra payroll **ARS 11.534.000**, máximo **ARS 11.520.000**, remanente **−ARS 14.000**, estado **EXCEEDED**. Son valores leídos del archivo humano, **no resultados validados de consolidación**. No se recalcularon con otro motor para aparentar éxito. Todos los caches de Summary se conservan en `observaciones_cache_excel.json`.

La aprobación sigue **PENDING_HUMAN_APPROVAL**. Compensation/coordinador debe resolver la incompatibilidad de representación de fórmulas mediante una corrección autorizada y una nueva ejecución. Esta evidencia del rechazo se conserva; no autoriza aprobar salarios o excesos.

Host: Codex desktop en la conversación existente. El system prompt del paquete se leyó como instrucciones de tarea; no sustituyó el system prompt del host. El user prompt específico archivado es la formulación operativa del pedido, no una exportación literal de API. No hubo una llamada adicional a API LLM. Modelo exacto, tokens y usage metadata no se expusieron de manera verificable y quedan null; no se estiman costos. `salida_agente.json` es la respuesta estructurada redactada por el agente después de leer los resultados; `ejecucion/reporte.json` conserva separadamente el reporte real de la herramienta.

Los datos salariales mantienen procedencia sintética: originales con hashes verificados, etiquetas SYNTHETIC DEMO, ningún nuevo texto de negocio en celdas y un único ajuste declarado. El Excel humano conserva metadatos de autor/última modificación, tal como fue recibido; no se utilizan como autenticación ni para identificar al reviewer sintético. La comprobación de procedencia no es un detector universal de confidencialidad.

La contraseña efímera de protección usada para la invocación sólo se pasó por entorno. Se buscó su valor exacto en archivos y partes ZIP/XLSX antes de descartarlo, sin hallazgos. Las copias pueden conservar verificadores de protección; no equivalen a contraseña en claro ni cifrado. Los documentos añadidos después sólo contienen resultados y código sin ese valor.

Se comprueba por SHA256 que V1–V4, Corrida 1, Corrida 3 y todos los archivos previamente versionados fuera de corrida_02 permanecen intactos. Los tests de esta corrida son de lectura/consistencia; no vuelven a consolidar ni editan Excel. El inventario SHA256 incluye todos los archivos de evidencia salvo el propio inventario.
